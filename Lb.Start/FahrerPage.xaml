<Page x:Class="Lb.Start.FahrerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Lb.Start"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FahrerPage">

    <Grid>
        <Grid Grid.ColumnSpan="3" Grid.Row="1">
            <ListView Name="lsvFahrer" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="100" Header="ID" DisplayMemberBinding="{Binding Path=ID}"/>
                        <GridViewColumn Width="500" Header="Vorname" DisplayMemberBinding="{Binding Path=Vorname}"/>
                        <GridViewColumn Width="500" Header="Nachname" DisplayMemberBinding="{Binding Path=Nachname}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

    </Grid>
</Page>
